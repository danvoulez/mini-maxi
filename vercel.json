{
	"buildCommand": "pnpm build",
	"devCommand": "pnpm dev",
	"installCommand": "pnpm install",
	"framework": "nextjs",
	"regions": ["iad1"],
	"functions": {
		"app/(chat)/api/**/*.ts": {
			"maxDuration": 60
		},
		"app/api/**/*.ts": {
			"maxDuration": 60
		}
	},
	"headers": [
		{
			"source": "/api/(.*)",
			"headers": [
				{
					"key": "Cache-Control",
					"value": "no-store, must-revalidate"
				}
			]
		}
	],
	"redirects": [
		{
			"source": "/app/(chat)/api/:path*",
			"destination": "/api/:path*",
			"permanent": false
		}
	]
}
